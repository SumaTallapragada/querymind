"""Phase 16 endpoint routers -- one module per HTTP resource.

Distinct from `querymind.api.v1.endpoints` (Phase 1's health-probe
router, unchanged) purely to keep this phase's additions self-contained
without touching that directory; `querymind.api.v1.router` includes
both.
"""
